//
// Created by Erimsah Olut on 21.06.2025.
//

#include <stdio.h>
#include <string.h>

void readScores(int a, int scores, int &n, int &m) {
}

int topProgrammer(int scores[],int n,int m,int a) {

    return 0;
}

int main() {
    int scores[15][12], n, m, best;
    readScores(argv[1], scores, &n, &m);
    best = topProgrammer(scores, n, m, atoi(argv[2]));


    return 0;
}
